# scikit-learn is pinned exactly: fertilizer_rf_model.pkl and
# fertilizer_scaler.pkl were produced by 1.6.1 and unpickle unreliably under a
# different version. The rest stay permissive so Streamlit Cloud can resolve
# them against whichever Python it provisions.
streamlit>=1.30
scikit-learn==1.6.1
joblib>=1.3
pandas>=2.1
numpy>=1.26,<3
